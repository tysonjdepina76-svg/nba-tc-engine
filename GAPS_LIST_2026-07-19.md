# Pipeline Gaps — 2026-07-19

## DATA / LINES (External APIs Dead)
1. **SDIO API key dead** — 401 on all odds endpoints. No WNBA/MLB lines.
2. **Odds API Business quota maxed** — No WC lines. Known limitation.
3. **SerpAPI 250 searches burned** — No per‑run cap existed. All quota consumed. 29‑day wait.
4. **Pipeline stuck at SELF_EDGE** — 0 real market lines flowing. 486 picks all self‑edge.
5. **WC picks not generating** — Missing `correct_wc_team` import (merged with `serp_odds_scraper` on same line).

## API ENDPOINTS
6. **19 dead/obsolete endpoints in main.py** — Purged. Included old `/api/combos/precomputed`, `/api/picks/yesterday`, `/api/roster`, `/api/projection`, `/api/trend-analysis`, `/api/recommendations`, `/dashboard` HTML route, etc.
7. **COMBO_DEFS deleted during purge** — Broke `/api/v1/combos` (NameError). Re‑added.
8. **Combos DB out of sync** — combos DB table had 16 entries, combos_daily.json had 54. Mismatched.

## DASHBOARD / UI
9. **Combos banner keeps regressing** — Inline combo detection banner on /nba‑tc game cards. Removed — Combos tab handles this.
10. **WC live stats dashboard empty** — BoxScoreView had no WC_COLS or WC rendering blocks. Added G,A,SH,SOT,PAS,TKL,YC columns.
11. **/dashboard COMBOS button broken** — Was fetching from old `/api/combos/precomputed` (WNBA‑only). Fixed to `/api/v1/combos`.
12. **c.label vs c.combo_label** — Field name mismatch in combo rendering. Fixed.
13. **/nba‑tc static Vite bundle** — Not hot‑reloading API changes. Needs rebuild to reflect fixes.
14. **Combo banner overflow/glow** — Visually intrusive, glow effects breaking card boundaries. Removed.
15. **Live box scores had no WC or MLB paths** — `live_boxscore.py` was WNBA‑hardcoded. Added WC + MLB ESPN endpoints.
16. **WC team name normalization missing** — `correct_wc_team()` not applied before projections.

## PIPELINE / CODE
17. **Import bug** — `correct_wc_teamfrom serp_odds_scraper` merged on one line. Split into two imports.
18. **No SerpAPI per‑run cap** — `serp_odds_scraper.py` had `PER_RUN_LIMIT = 8` but was never enforced in `enrich_lines_via_serpapi()`.
19. **daily_picks.py arg parsing** — `--sport wc` not fully wired. Added to ArgParser choices.
20. **Build pregame combos not running for WC** — `build_pregame_combos.py` needed WC COMBO_DEFS populated.
21. **Workspace clutter** — Empty dirs, .bak files, stale node_modules. Cleaned.

## CURRENT STATE
- **11 live endpoints** all return 200 OK
- **30 projection files** generated (WNBA + MLB + WC)
- **3 dashboards** wired to correct endpoints
- **Pipeline runs** but generates SELF_EDGE picks only
- **Blocked until** SerpAPI quota resets (29 days) or new API key obtained
