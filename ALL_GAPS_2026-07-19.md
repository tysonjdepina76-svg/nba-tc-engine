# All Gaps — TC Pipeline — 2026-07-19

## ACTIVE / UNRESOLVED

1. SerpAPI quota burned — no cap was in code, burner ran 100+ searches per run, exhausted free tier. 29 days until reset.
2. SDIO API key dead — returns 401 on all odds endpoints. Key invalid or tier doesn't include odds.
3. Odds API Business tier quota maxed — /odds/ and /props/ return 401.
4. SGO API doesn't support WNBA (NBA-only tier).
5. All 721 picks are SELF_EDGE — zero real market lines from any source.
6. WC projection files duplicated (.json at/with @ separator).
7. WC live box scores return empty — live_boxscore.py has no WC game support (only WNBA/MLB).
8. WC picks line=0 across all 64 picks because WC has no book lines and SerpAPI can't fetch.
9. daily_picks.py import bug — merged `correct_wc_teamfrom serp_odds_scraper` on one line (fixed but was a gap).
10. SerpAPI enrichment searches every zero-line/generic pick — no edge-priority sorting, no daily cap.

## FIXED

11. /api/v1/combos returned mixed dates — no date filter (fixed: filters by today).
12. /api/v1/combos COMBOS button on /dashboard fetched from old /api/combos/precomputed (WNBA-only) (fixed: now uses /api/v1/combos with all leagues).
13. Dashboard rendered c.label which doesn't exist in combo API (fixed: c.combo_label || c.combo).
14. WC live games tab had no WC column definitions or rendering blocks (fixed: added WC_COLS and rendering).
15. Combo banner overflow/glow was visually intrusive on game cards (fixed: compact pill style).
16. /api/v1/combos endpoint broke after purge — COMBO_DEFS deleted accidentally (fixed: restored COMBO_DEFS and _build_combos_from_db).
17. Workspace had empty directories, .bak files, stale node_modules (cleaned).
18. generate_projections.py was generating fake book lines (fixed: line=0 across all sports).
19. SerpAPI-enriched picks weren't propagating signal=SERPAPI to DB (fixed).

## API PURGE (19 dead endpoints removed)

20. /api/combos/precomputed → migrated to /api/v1/combos
21. /api/v1/combos/rebuild → not used
22. /api/v1/stats/{sport} → not used
23. /api/v1/projection/{sport} → not used
24. /api/picks/recent → not used
25. /api/picks/yesterday → not used
26. /api/stats/picks-by-sport → not used
27. /api/stats/hit-rate → not used
28. /api/stats/daily-summary → not used
29. /api/stats/edge-distribution → not used
30. /api/backtest/week → not used
31. /api/backtest/month → not used
32. /api/backtest/sport/{sport} → not used
33. /api/system-data → not used
34. /api/refresh-data → not used
35. /api/injuries → not used
36. /api/lines/{sport} → not used
37. /api/system/collect → not used
38. /dashboard (HTML) → not used

## PIPELINE STATE

- 11 endpoints now live, all return 200 OK
- Dashboards: /nba-tc, /dashboard, / home page all use only surviving endpoints
- 721 projections generated today (WNBA + MLB + WC)
- All picks = SELF_EDGE (no real lines)
- Combo DB has data but no real edge without market lines
- Code is clean, next step is getting working API keys
